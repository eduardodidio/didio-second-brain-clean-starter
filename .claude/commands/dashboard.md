---
description: Open the claude-didio-config monitoring dashboard in the browser
---

Run `didio dashboard` to start the local monitoring dashboard and open it
in the default browser. If the dashboard has not been built yet (Phase 2),
fall back to printing the most recent agent meta logs.

```bash
didio dashboard
```
