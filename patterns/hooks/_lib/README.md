# patterns/hooks/_lib/

Helpers sourceados pelos hook.sh via `source "$(dirname "$0")/../_lib/<helper>"`.

Primeiro helper: `registry-match.sh` — filtra execução por `$CLAUDE_PROJECT_DIR` contra `projects/registry.yaml`. Ver F05b-T04.
