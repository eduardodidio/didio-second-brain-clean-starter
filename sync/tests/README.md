# sync/tests/

**Status:** populated ongoing via F05
**Responsible feature:** F05 (slim)

Test harness para `sync/lib.sh`, `install-mcp-in-project.sh` e
`install-discord-hooks.sh`. Helper de assertions em `assert.sh`.

Rodar tudo: `bash sync/tests/run-all.sh` (criado em F05-T04).
