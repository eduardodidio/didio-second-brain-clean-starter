Hermetic JSONL fixtures for F15 token-report scripts. Three scenarios: **happy** (`session-aaa.jsonl` — 3 records, 2 models: sonnet-4-6 + opus-4-7), **MCP-heavy** (`session-bbb.jsonl` — 4 lines with `mcp__second-brain__memory_search` and `mcp__second-brain__knowledge_get` tool_use calls), and **malformed** (`session-ccc.jsonl` — 2 valid JSON lines + 1 invalid line to test error-tolerant pipelines). Point token-collector.sh at this root via `--transcripts-root _bootstrap/scripts/_lib/fixtures/token-report/projects/` when running tests.
